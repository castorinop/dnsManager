{% for z in rows %}
	{% if z.type == 'master' %}
<div>
	<br/>
	<code>
	
	# file "{{ z.type }}/{{ z.view }}-{{z.domain }}";<br/>
	{{ include('dnsManagerBundle:Server:empty.zone.twig') |nl2br }}
	</code>
</div>
	{% endif %}
{% endfor %}
