{% extends 'ToaTwitterBootstrapBundle::layout.html.twig' %}
{% block title %} Edit Zone {% endblock %}
{% block content %}
		<form class="form-horizontal" action="{{ path('dns_manager_zone_update', {'domain': domain}) }}" method="post" {{ form_enctype(form) }}>
		<div>			
				{{ form_widget(form) }}
		</div>
			<button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
			<button class="btn btn-primary">Save changes</button>
		</div>
		</form>
{% endblock %}